# Copy this file to .env and fill in your values
# DO NOT commit .env — it contains secrets

# Gemini API Key (required for AI fix generation and chat)
GEMINI_API_KEY=your_gemini_api_key_here

# GitHub Personal Access Token (required for branch creation and PR)
# Needs scopes: repo (full), workflow
GITHUB_TOKEN=your_github_pat_here

# Optional: override the backend port (default: 8000)
# PORT=8000

# Optional: override CORS allowed origin (default: http://localhost:5173)
# FRONTEND_URL=http://localhost:5173
